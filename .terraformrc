# 离线插件配置
# 将此文件放到 ~/.terraformrc 或项目根目录的 .terraformrc

provider_installation {
  filesystem_mirror {
    path    = "/terraform-plugins"
    include = ["*/*"]
  }
  direct {
    exclude = ["*/*"]
  }
}
